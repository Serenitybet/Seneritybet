[phases.setup]
nixPkgs = ["nodejs_20"]

[phases.install]
cmds = [
  "npm install -g pnpm@9.15.4",
  "pnpm install --frozen-lockfile"
]

[phases.build]
cmds = [
  "pnpm --filter @serenitybet/db db:generate"
]

[start]
cmd = "pnpm --filter @serenitybet/api start:prod"
